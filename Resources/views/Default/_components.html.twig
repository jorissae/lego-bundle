{%  include 'IdkLegoBundle:Default:_flash.html.twig' %}

<div  data-widget-order-save="{{ configurator.getUrl('ordercomponents', {'suffix_route': configurator.currentComponentSuffixRoute}) }}" class="jsa-widget-container">
    {% for component in components %}
        <div data-widget-id="{{ component.id }}" id="{{ component.id }}" class="jsa-widget  {{ component.class }}">{% include component.template with component.templateAllParameters %}</div>
    {%  endfor %}
</div>