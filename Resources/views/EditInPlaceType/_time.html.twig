<input  class="lego-edit-in-place-input" id="{{ component.gid('input') }}-{{ field.name }}-{{ item.id }}" type="text" value="{{ value| date('H:i') }}"/>
<script>
    $(function(){
        $('#{{ component.gid('input') }}-{{ field.name }}-{{ item.id }}').timepicker({
            'timeFormat':'HH:mm',
        });
    });
</script>