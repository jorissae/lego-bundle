{% if '\n' in value %}
    <textarea  class=" form-control col-md-12" id="{{ component.gid('input') }}-{{ field.id }}-{{ item.id }}">{{ value }}</textarea>
{% else %}
    <input  class="lego-edit-in-place-input" id="{{ component.gid('input') }}-{{ field.id }}-{{ item.id }}" type="text" value="{{ value }}"/>
{% endif %}